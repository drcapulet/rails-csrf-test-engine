RSpec.describe Rails::Csrf::Test::Engine do
  it "has a version number" do
    expect(Rails::Csrf::Test::Engine::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
